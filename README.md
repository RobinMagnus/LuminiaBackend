# Fluxo de branching

- Criar branch `feature/*` a partir da branch `develop`.
- Abrir Pull Request (PR) para `develop`.
- Depois de testado em `develop`, abrir PR de `develop` para `main`.
- Recomendação: proteger a branch `main` exigindo os checks `build`, `test` e 1 aprovação de `@RobinMagnus`.
